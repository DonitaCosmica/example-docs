//@ts-check

/**
 * Lista de Edades de usuarios
 * @type {Array}
 */
const age = [19, 20, 30, 100];

/**
 * Array of User's Scores
 * @type {Array<Number>}
 */
const scores = [10, 199.99, 299.99, 3000];

/**
 * Array of Number and Strings
 * @type {Array<Number | String>}
 */
const scoresAndStrings = [10, "10", "30", 3000];

/**
 * Array of names
 * @type {Array<String>}
 */
const names = ["Ray", "James", "Joe"];

names.push("Dona");